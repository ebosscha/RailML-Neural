<UserControl x:Class="RailMLNeural.UI.RailML.Views.OCPTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid" 
        mc:Ignorable="d ignore"
        DataContext="{Binding OCPTableVM, Source={StaticResource Locator}}">

    <Grid>
        <xcdg:DataGridControl ItemsSource="{Binding OCPs}" 
                              AutoCreateColumns="False"
                              SelectionChanged="DataGridControl_SelectionChanged"
                              SelectionMode="Single"
                              HorizontalAlignment="Left" VerticalAlignment="Top">
            <xcdg:DataGridControl.Columns>
                <xcdg:Column FieldName="id"
                             Title="ID"/>
                <xcdg:Column FieldName="name"
                             Title="Name"/>
                <xcdg:Column FieldName="description"
                             Title="Description"/>
            </xcdg:DataGridControl.Columns>
        </xcdg:DataGridControl>

    </Grid>
</UserControl>
