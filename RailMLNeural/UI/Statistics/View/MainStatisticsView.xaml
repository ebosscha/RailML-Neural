<UserControl x:Class="RailMLNeural.UI.Statistics.View.MainStatisticsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        DataContext="{Binding MainStatisticsVM, Source={StaticResource Locator}}">

    <Grid>
        <xcad:DockingManager>
            <xcad:LayoutRoot>
                <xcad:LayoutPanel>
                    <xcad:LayoutDocumentPane>
                        <xcad:LayoutDocument Title="Network"
                                             CanClose="False">
                            <ContentControl Content="{Binding NetworkStatistics}"/>
                        </xcad:LayoutDocument>
                        <xcad:LayoutDocument Title="Delays"
                                             CanClose="False">
                            <ContentControl Content="{Binding DelayStatistics}"/>
                        </xcad:LayoutDocument>
                    </xcad:LayoutDocumentPane>
                </xcad:LayoutPanel>
            </xcad:LayoutRoot>
        </xcad:DockingManager> 
    </Grid>
</UserControl>
